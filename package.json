{
    "name": "grooveshare",
    "private": true,
    "workspaces": [
        "client",
        "server",
        "packages/*"
    ],
    "scripts": {
        "test-client": "npm run typecheck -w client && npm test -w client",
        "test-server": "npm run typecheck -w server && npm test -w server",
        "test-runner": "npm run typecheck -w packages/test-runner && npm test -w packages/test-runner",
        "test-all": "npm run typecheck --workspaces --if-present && npm test --workspaces --if-present",
        "db:check": "npm run db:check -w server"
    }
}